language: R
cache: packages
sudo: false

env:
  - PKG_PATH=$TRAVIS_BUILD_DIR/GPfit-package/GPfit
  - R_LIBS=$TRAVIS_BUILD_DIR/GPfit-package/temp-lib

addons:
  apt:
    packages:
      - zlib1g-dev

script:
  - mkdir $R_LIBS
  - Rscript $TRAVIS_BUILD_DIR/GPfit-package/installation.R
